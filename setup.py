# Setup configuration for editable installs
from setuptools import setup, find_packages

setup(
    name="industrial-defect-detection",
    version="1.0.0",
    packages=find_packages(),
    python_requires=">=3.10",
)
